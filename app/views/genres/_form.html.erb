<%= form_with(model: genre) do |f| %>
  <%= render "shared/errors", objekt: genre %>
  <%= f.label(:name) %>
  <%= f.text_field(:name, autofocus: true) %>
  <%= f.collection_check_boxes(:movie_ids, Movie.order(:title), :id, :title) %>
  <%= f.submit %>
<% end %>
